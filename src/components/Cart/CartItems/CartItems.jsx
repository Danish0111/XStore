import React from 'react'
import { Contaxt } from '../../../Utils/Contaxt'
import { useContext } from 'react'
const CartItems = () => {
    const {CartItems, handleRemoveFromCart, handleCartQuantity} = useContext(Contaxt);
  return (
    <>
      
    </>
  )
}

export default CartItems
